#include <bits/stdc++.h>
using namespace std; 

int main(){
	//{key, value}
	//everything same as map, only it can store multiple keys 
	// only mapp[key] cannot be used here
	//~ {1,2} {1,2}  
	return 0;
}


