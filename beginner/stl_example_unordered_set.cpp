#include <bits/stdc++.h>
using namespace std;

int main(){
	//stores all the unique elemens but not in sorted order
	//lower_bound and upper_bound functions does not work
	//~ it does not sotore any particular order
	//~ it has better complexity than sets in most cases, 
	//~ except when collisions happen
	
	return 0;
}




